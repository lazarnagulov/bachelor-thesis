#import "../funkcije.typ": todo
= Будући рад
Архитектура система поставља основу за проширења. Модуларни дизајн предњег и задњег дела преводиоца омогућава идентификацију праваца за развој.

== Унапређење система компилације <будући_рад_аутоматизација>
Тренутна имплементација захтева ручно покретање преводилаца за сваки модул. Будућа верзија система треба да уведе механизам за инкременталну компилацију који би аутоматизовао процес. Систем би пратио измене у изворним _.testa_ датотекама помоћу пратилаца датотека (енг. _file watchers_) и поредио временске ознаке (енг. _timestamps_) изворног кода са постојећим бинарним модулима.

Приликом детекције измене, преводилац би ажурирао само оне модуле који су измењени. Интеграција детекције измене уклонила би потребу за мануелном интервенцијом и омогућила конзистентност између изворног и преведеног кода током развоја.

== Средњенивојска међурепрезентација

Тренутна архитектура преводи процесни низ директно из AST структуре у HIR. Имплементација MIR-а омогућила би увођење додатних фаза оптимизације кода. MIR служи као канонички приказ програма где се сложени изрази декомпонују у низ једноставних, линеарних операција.

Увођење MIR-а омогућава имплементацију анализа попут анализе тока података (енг. _data-flow analysis_) или оптимизације „мртвог кода“ (енг. _dead code elimination_). Прелазак на двостепену IR (HIR за семантичку анализу, MIR за оптимизацију) доприноси модуларности преводиоца. Процес генерисања излазног кода постаје независан од специфичности AST-а, што олакшава додавање нових бекенд циљева (нпр. инфраструктура за генерисање машинског кода (енг. _Low-Level Virtual Machine_) или генерација C кода).