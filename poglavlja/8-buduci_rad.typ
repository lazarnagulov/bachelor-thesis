#import "../funkcije.typ": todo
= Будући рад
Архитектура система поставља основу за даља проширења. Модуларни дизајн предњег и задњег дела преводиоца омогућава идентификацију праваца за даљи развој.

== Унапређење система превођења <будући_рад_аутоматизација>
Тренутна имплементација захтева ручно покретање преводиоца за сваки модул. Будућа верзија система требало би да уведе механизам за инкременталну компилацију који би аутоматизовао процес превођења. Систем би пратио измене у изворним _.testa_ датотекама помоћу механизма за праћење промена (енг. _file watchers_) и поредио временске ознаке (енг. _timestamps_) изворног кода са постојећим бинарним модулима.

Приликом детекције измене, преводилац би ажурирао само оне модуле који су измењени или који зависе од измењеног кода. Интеграција механизма детекција измене уклонила би потребу за мануелном интервенцијом и осигурала конзистентност између изворног и преведеног кода током развоја.
== Средњенивојска међурепрезентација
Тренутна архитектура преводи изворни код директно из AST структуре у HIR. Имплементација MIR-а омогућила би увођење додатних фаза оптимизације. MIR служи као канонички приказ програма у којем се сложени изрази декомпонују у низ једноставних, линеарних операција.

Увођење MIR-а отвара могућност за имплементацију анализа попут анализе тока података (енг. _data-flow analysis_) и елиминације „мртвог кода“ (енг. _dead code elimination_). Прелазак на вишестепену архитектуру IR-а (HIR за семантичку анализу, MIR за оптимизацију) додатно би побољшао модуларност преводиоца. Процес генерисања излазног кода постао би независан од специфичности AST-а, што би олакшало додавање нових _бекенд_ циљева, попут инфраструктуре за генерисање машинског кода (енг. _Low Level Virtual Machine_) или директног генерисања C кода.