#import "../funkcije.typ": todo
= Имплементација
Oво поглавље приказује имплементацију развијеног DSL-а и пратећег језичког сервера. Све компоненте система реализоване су у програмском језику _Rust_. Реализација прати фазе дефинисане у поглављу о дизајну. Лексички анализатор преводи изворни текст у ток токена (поглавље #ref(<лексичка_анализа>)). Синтаксни анализатор трансформише ток токена у AST (поглавља #ref(<парсирање>) и #ref(<аст>)). Компонента за семантичку анализу врши валидацију над генерисаним стаблом (поглавље #ref(<имплементација_семантичка_анализа>)).
Систем преводи AST у HIR (поглавље #ref(<високонивојска_међурепрезентација>)).

Сваки модул развијен је као независна целина. Модули користе централизоване механизме за управљање грешкама и дијагностиком. Архитектура подржава размену података са језичким сервером. Исправност сваке компоненте потврђена је кроз јединичне и интеграционе тестове.

== Лексичка анализа <лексичка_анализа>
Лексичка анализа представља прву фазу превођења. Циљ је трансформисати изворни текст у низ токена. Имплементација користи итератор _Peekable\<CharIndices\>_. Итератор омогућава обраду текста уз могућност прегледа наредних карактера без померања позиције показивача @ruststd. Увид у наредне карактере омогућава препознавање вишекарактерних оператора попут `&&`, `||`, `..` и `..=`. Препознавање се извршава без алокације додатног стања у меморији.

За препознавање кључних речи користи се регистар _KEYWORD_REGISTRY_. Регистар је имплементиран помоћу статичке мапе _once_cell::sync::Lazy_ @ruststd. Мапа обезбеђује константно време претраге приликом идентификације резервисаних речи. Сваки токен чува позицију из изворног кода унутар структуре _Span_. Структура _Span_ садржи почетну и крајњу линију и колону. Подаци о позицији омогућавају језичком серверу мапирање дијагностичких порука на оригинални текст.

Структура _LexerError_ служи за мапирање лексичких неправилности у дијагностичке поруке. Лексички анализатор не прекида рад при наиласку на невалидан карактер или нетерминиран литерал.Компонента генерише грешку и наставља обраду преосталог дела датотеке. Јединични тестови у датотеци _tests.rs_ потврђују исправност рада лексичког анализатора. Тестови покривају примитивне токене, сложене операторе и граничне случајеве попут недовршених стрингова. Лексички анализатор обезбеђује структуриране податке за потребе синтаксног анализатора.

== Синтаксна анализа <парсирање>
Синтаксни анализатор трансформише низ токена у AST. Имплементација је подељена на засебне датотеке попут _expression.rs_, _statement.rs_ и _token_stream.rs_. Централна структура _Parser_ управља целокупним процесом синтаксне анализе.

=== Управљање током токена
Компонента _TokenStream_ омогућава предиктивну анализу и вирење унапред (енг. _lookahead_) (листинг @lst:token_stream). Синтаксни анализатор прегледа наредне токене без њиховог уклањања из примарног тока. Преглед токена унапред неопходан је за разликовање литерала и типова података.

#figure(
  ```rs
  pub struct TokenStream<I: Iterator<Item = Result<Token, LexerError>>>
  {
      lexer: Peekable<I>,
      buffer: VecDeque<Token>,
      last_span: Span,
  }
  ```,
  caption: [Структура _TokenStream_ компоненте]
) <lst:token_stream>

_TokenStream_ користи итератор _Peekable_ и интерни бафер _VecDeque_ за привремено складиштење токена. Архитектура омогућава управљање стањем тока и прескакање токена током синхронизације. Поље _last_span_ служи за праћење последње познате позиције у изворном коду.

=== Синтаксна анализа израза
Обрада израза ослања се на Пратов (енг. _Pratt_) синтаксни анализатор. Функција _parse_expression_ прихвата аргумент _precedence_ за одређивање приоритета оператора. Логика парсирања подељена је на две фазе. Прва фаза користи позив функције _parse_primary_expression_ за обраду атомских вредности попут литерала, идентификатора и заграда. Друга фаза обухвата итеративну проверу наредног токена и поређење са тренутним нивоом приоритета. Синтаксни анализатор позива функцију _parse_infix_expression_ за операције над већ обрађеним изразима.

=== Синтаксна анализа исказа
Функција _parse_statement_ усмерава ток извршавања на основу врсте токена. Функција позива метод _parse_directive_ за кључне речи `output`, `seed` и `import`. Методи _parse_template_ и _parse_struct_ обрађују шаблоне и структуре. Методе _parse_enum_ и _parse_type_declaration_ парсирају енумерације и декларације типова. Искази представљају заокружене семантичке целине попут дефиниција структура или шаблона. Изрази се парсирају унутар блокова _ExpressionStatement_ са обавезном тачком-зарезом на крају.

=== Синтаксна анализа типова података и ограничења
Синтаксни анализатор обрађује типове података кроз систем наменских функција. Функција _parse_type_ препознаје примитивне типове, листе и кориснички дефинисане типове. Систем подржава додавање семантичких ограничења на типове података. Ограничења се синтаксички дефинишу унутар угластих заграда. Регистар _CONSTRAINTS_ садржи листу свих дозвољених врста ограничења. Подржана ограничења укључују опсег, минималну вредност, максималну вредност и дужину. Регистар користи статичку мапу за валидацију идентификатора ограничења током парсирања. Непозната ограничења изазивају прекид парсирања тренутног израза и генерисање одговарајуће грешке.

=== Синтаксна анализа текстуалних шаблона
Језик подржава валидацију стрингова помоћу специјализованих текстуалних шаблона. Функција _parse_string_pattern_ анализира садржај текстуалног литерала карактер по карактер. Синтаксни анализатор користи итератор _Peekable\<CharIndices\>_ за навигацију кроз садржај шаблона. Механизам из литерала издваја константне делове и специјалне услове. Услови дефинишу правила попут понављања малих слова, великих слова или бројева. Број понављања карактера може бити дефинисан динамички. Динамички број понављања захтева инстанцирање секундарног лексичког и синтаксног анализатора. Секундарни синтаксни анализатор обрађује угњеждене изразе унутар угластих заграда шаблона.

=== Руковање грешкама и синхронизација
Опоравак од грешака реализован је кроз метод _synchronize_. Синтаксни анализатор наставља рад након појаве грешке _ParserError_. Компонента прескаче токене унутар _TokenStream_-а до следећег поузданог граничника. Као граничници служе тачка-зарез или почетак новог исказа. Синхронизација омогућава пријаву више независних синтаксних грешака током једног покретања.

=== Тестирање синтаксне анализе
Исправност синтаксног анализатора осигурана је кроз скуп аутоматизованих тестова. Јединични тестови проверавају рад синтаксног анализатора у изолованом окружењу. Изоловано тестирање потврђује правилну конструкцију AST-а за сваки појединачни исказ. Интеграциони тестови верификују правилну комуникацију између лексичког и синтаксног анализатора. Аутоматизовани тестови спречавају регресије током даљег развоја синтаксних правила.

== Апстрактно синтаксно стабло <аст>
AST је имплементирано као модуларна архитектура унутар датотеке _mod.rs_. Систем је подељен на логичке целине за изразе, исказе, варијанте, поља и ограничења. Централни чвор за репрезентацију израза у коду је структура _Expression_. Структура _Expression_ и припадајућа енумерација _ExpressionKind_ приказане су на листингу @lst:ast_expression.

#figure(
  ```rust
  pub struct Expression {
      pub kind: ExpressionKind,
      pub span: Span,
  }

  pub enum ExpressionKind {
      IntLiteral(isize),
      StringLiteral(String),
      BooleanLiteral(bool),
      Identifier(String),
      List(Vec<Element>),
      Type(DataType),
      Reference {
          template: String,
          field: String,
      },
      // ... остале варијанте су изостављене ради прегледности
  }
  ```,
  caption: [Структура и енумерација за репрезентацију језичких израза],
) <lst:ast_expression>

Рекурзивни чворови за префиксне и инфиксне операторе омогућени су употребом паметних показивача (енг. _smart pointer_) _Box_ @ruststd. Рад са листама имплементиран је преко структуре _Element_. Структура _Element_ дозвољава придруживање тежинских израза члановима листа. Структура програма и синтаксне наредбе представљене су кроз енумерацију _Statement_. Свака варијанта унутар енумерације _Statement_ чува метаподатке о позицији и структури изворног кода. Искази који садрже самосталан израз издвајају се унутар наменске структуре _ExpressionStatement_. За потребе претраге, валидације и трансформације генерисаног стабла развијен је интерфејс _Visitor_. Интерфејс дефинише методе за посету сваког појединачног типа чвора у AST-у. Функције _walk_program_, _walk_statement_ и _walk_expression_ омогућавају рекурзивно кретање кроз дубину стабла. Помоћне функције усмеравају посетитеља на угњеждене подчворове израза, варијанти и поља.

== Семантичка анализа <имплементација_семантичка_анализа>
Семантичка анализа врши контекстуалну валидацију AST-a након синтаксне анализе. Процес осигурава испуњеност правила која превазилазе могућности контекстно-слободних граматика. Архитектура система заснива се на вишефазном модуларном приступу. Имплементација користи дизајнерски шаблон посетилац за обилазак језичких конструкција. Структура _SemanticAnalyser<'a>_ оркестрира процес анализе кроз методу _analyse_with_imports_. Систем као резултат генерише структуру _AnalysisResult_. Излазна структура садржи табелу симбола, мапу референци, мапу типова и колекцију дијагностичких порука.

=== Фазе семантичке анализе
Процес семантичке анализе обухвата пет секвенцијалних фаза. 
1. Прва фаза подразумева изградњу табеле симбола кроз структуру _SymbolTableBuilder_. Компонента региструје глобалне и локалне симболе уз успостављање хијерархије опсега важења. Детекција дуплираних декларација прекида процес анализе пре покретања наредних фаза. 
2. Друга фаза извршава детекцију циклуса у наслеђивању шаблона. Систем проверава постојање кружних зависности пре почетка валидације поља и референци. 
3. Трећа фаза обухвата проверу референци помоћу компоненте _ReferenceChecker_. Анализатор потврђује постојање сваког идентификатора у тренутном контексту или унутар увезених модула. 
4. Четврта фаза мапира међусобне везе објеката кроз структуру _ReferenceTracker_. Прикупљени метаподаци служе за очување референцијалног интегритета током генерисања података. 
5. Пета фаза извршава проверу типова и ограничења помоћу компоненте _TypeChecker_. Систем закључује типове израза, детектује некомпатибилност оператора и валидира семантичку исправност ограничења.

=== Табела симбола и управљање опсезима важења
Структура _SymbolTable_ управља симболима кроз организацију стабла повезаних опсега. Сваки опсег садржи идентификатор _ScopeId_, мапу локалних симбола и показивач ка родитељском опсегу. Лексичко претраживање симбола од локалног ка глобалном нивоу извршава се позивом методе _lookup_.

Систем дефинише врсте опсега кроз енумерацију _ScopeKind_. _Global_ је врховни опсег за декларације шаблона, структура, енумерација и типова. _Template_ дефинише опсег унутар једног шаблона. _Enum_ ограничава контекст унутар дефиниције енумерације. _Generate_ означава опсег унутар блока за генерацију података. _Directive_ и _Block_ служе као помоћни локални опсези.

Симболи су представљени структуром _Symbol_. Полиморфна структура за чување метаподатака о језичким конструктима приказана је на листингу @lst:symbol_kind.

#figure(
  ```rust
  pub enum SymbolKind {
      Template {
          fields: Vec<String>,
          parent: Option<String>,
      },
      Struct {
          name: String,
          fields: Vec<Field>,
      },
      Enum {
          variants: Vec<VariantInfo>,
      },
      TypeAlias {
          name: String,
          data_type: Expression,
      },
      Field {
          template_name: String,
          is_override: bool,
          expression: Expression,
      },
  }
  ```,
  caption: [Енумерација _SymbolKind_ за репрезентацију семантичких метаподатака],
) <lst:symbol_kind>

=== Детекција циклуса у наслеђивању
Структура _SymbolTable_ имплементира алгоритам за детекцију циклуса унутар методе _check_inheritance_cycle_. Алгоритам спречава бесконачну рекурзију приликом кружног наслеђивања шаблона. Претрага пролази кроз ланац наслеђивања праћењем референци ка родитељима. Алгоритам користи хеш-скуп за праћење посећених чворова. Поновно појављивање истог шаблона унутар тренутне путање прекида извршавање претраге. Метод враћа листу стрингова са тачним редоследом цикличне зависности. Анализатор трансформише резултат у дијагностичку грешку _SemanticError::InheritanceCycle_.

=== Провера типова и семантичких ограничења
Компонента _TypeChecker_ извршава валидацију типова израза унутар AST-a. Модул имплементира интерфејс посетитеља за аутоматско пресретање и анализу чворова стабла.

Компонента извршава валидацију тежинских фактора унутар дефиниција енумерација. Метод _visit_variant_ захтева целобројни тип израза за дефинисање тежине. Одступање од целобројног типа резултује грешком о неслагању типова. Анализатор контролише команде за генерацију података кроз метод _visit_generate_. Израз за дефинисање броја генерисаних инстанци мора припадати целобројном типу. Систем анализира изразе ограничења попут _Range_, _Min_, _Max_, _Length_ и _MultipleOf_. Семантички анализатор проверава компатибилност ограничења са тренутним типом података.

=== Дијагностика и руковање грешкама
Семантички анализатор спроводи стратегију акумулације грешака уместо прекидања извршавања при првој неправилности. Систем чува све пронађене нерегуларности унутар колекције грешака структуре _SemanticAnalyser_.

Семантичке грешке су типизиране кроз енумерацију _SemanticError_. Дефиниција типизираних семантичких грешака и метод за њихову трансформацију у дијагностички формат приказани су на листингу @lst:semantic_error.

#figure(
  ```rust
  pub enum SemanticError {
      UnknownIdentifier {
          name: String,
          span: Span,
      },
      TypeMismatch {
          expected: String,
          found: String,
          span: Span,
      },
      // ... остале варијанте грешака
  }

  impl SemanticError {
      pub fn to_diagnostic(&self) -> Diagnostic {
          match self {
              SemanticError::TypeMismatch { expected, found, span } => {
                  Diagnostic::error(
                      *span,
                      format!("Type mismatch: expected '{}', found '{}'", expected, found),
                  )
                  .with_code(DiagnosticCode::TypeMismatch)
                  .with_hint("Ensure the expression matches the expected type")
              }
              // ... мапирање осталих грешака
          }
      }
  }
  ```,
  caption: [Дефиниција семантичких грешака и мапирање у објекте дијагностике],
) <lst:semantic_error>

Свака варијанта садржи структуру _Span_ за мапирање координата у изворном коду. Метода _to_diagnostic_ трансформише објекат семантичке грешке у LSP дијагностички формат. Систем генерише контекстуалне савете за исправку на основу специфичног типа грешке.

== Високонивојска међурепрезентација <високонивојска_међурепрезентација>
HIR је упрошћена форму програма након завршене семантичке анализе. Трансформација из AST-а у HIR уклања синтаксне конструкције и нормализује структуру података. HIR служи као основа за даљу оптимизацију, проверу компатибилности и серијализацију модула. Инфраструктура за управљање меморијом и дијагностиком приказана је на листингу @lst:hir_infra.

#figure(
    ```rust
        pub struct StringPool {
            strings: Vec<String>,
            map: HashMap<String, StringId>,
        }

        pub struct SpanMap<K, V> {
            entries: Vec<(K, V)>,
            cache: OnceLock<HashMap<K, V>>,
        }
    ```,
caption: [Инфраструктура за меморијску оптимизацију и мапирање изворног кода],
) <lst:hir_infra>

Интернирање стрингова преко структуре _StringPool_ обезбеђује репрезентацију идентификатора. _SpanMap_ омогућава повезивање метаподатака са позицијама у изворном коду. Структура користи _OnceLock_ за лењо кеширање претрага ради убрзања приступа координатама. Поступак трансформације изводи компонента _AstLowering_. Имплементација структуре _AstLowering_ приказана је на листингу @lst:lowering_struct.

#figure(
  ```rust
    pub struct AstLowering {
        context: LoweringContext,
        string_pool: StringPool,
        source_map_builder: SourceMapBuilder,
        directives: Vec<Directive>,
        source_file: PathBuf,
        items: Vec<Item>,
    }
  ```,
  caption: [Структура _AstLowering_ задужена за трансформацију AST-а у HIR],
) <lst:lowering_struct>

Компонента _AstLowering_ користи _string_pool_ за дедупликацију текста и _source_map_builder_ за праћење порекла чворова. Метода _lower_expr_ рекурзивно обилазе чворове AST-a. Генерисани модел омогућава бинарну серијализацију модула.

== Систем модула
Систем модула омогућава декомпозицију програма у независно преводиве целине. Сваки модул представља јединицу компилације која се након анализе серијализује у бинарни формат _.tmod_. Архитектура подржава увоз симбола кроз систем референци и мапирање идентификатора.

=== Глобално адресирање и серијализација
Физичка локација ставки апстрахована је коришћењем униформног глобалног адресирања. Идентификација симбола у меморији ослања се на композитне нумеричке кључеве (листинг @lst:global_id).

#figure(
    ```rust
        #[derive(Copy, Clone, Hash, Eq, PartialEq)]
        pub struct LocalItemId(pub u32);

        #[derive(Copy, Clone, Hash, Eq, PartialEq)]
        pub struct ModuleId(pub u32);

        #[derive(Copy, Clone, Hash, Eq, PartialEq)]
        pub struct GlobalItemId {
            pub module: ModuleId,
            pub item: LocalItemId,
        }
    ```,
    caption: [Структуре података за глобално адресирање ставки],
) <lst:global_id>

Структура _LocalItemId_ одређује ставку унутар граница једне датотеке. Структура _GlobalItemId_ спаја локални идентификатор са идентификатором модула (_ModuleId_). Глобално адресирање омогућава униформан рад са локалним и спољашњим симболима.

Бинарни формат модула остварен је коришћењем библиотеке _rmp_serde_ @rmpserde. _Rmp_serde_ омогућава компактну серијализацију _Rust_ структура у _MessagePack_ формат. Нумерички кључеви се пресликавају у бинарне репрезентације, што смањује заузеће простора на диску.

=== Резолуција, учитавање и релокација
Компонента _ModuleResolver_ управља процесoм проналажења и учитавања зависности у меморију. Претрага обухвата директоријуме стандардне библиотеке и локалне путање пројекта. Имплементација методе за разрешавање зависности приказана је на листингу @lst:resolver.

#figure(
    ```rust
    pub struct ModuleResolver {
        search_paths: Vec<PathBuf>,
        cache: HashMap<String, Module>,
        next_module_id: u32,
    }

    impl ModuleResolver {
        fn resolve_one(
            &mut self, 
            name: &str, 
            source_path: &Path
        ) -> Result<(), ResolveError> {
            if self.cache.contains_key(name) { return Ok(()); }
            
            let path = self.find_tmod(name, source_path)?;
            let mut module = Module::load(&path)?;
            let allocated_id = ModuleId(self.next_module_id);
            self.next_module_id += 1;
            self.cache.insert(name.to_string(), module);
            
            Ok(())
        }
    }
    ```,
    caption: [Логика разрешавања, валидације кеша и релокације модула],
) <lst:resolver>

_ModuleResolver_ користи хеш-мапу за мемоизацију. Мемоизација спречава поновно учитавање истог модула. У случају неуспешне резолуције или грешке при читању, систем генерише _ResolveError_ са подацима о узроку.

=== Серијализација и интегритет података
Бинарни формат модула остварен је коришћењем библиотеке _rmp_serde_ @rmpserde. _Rmp_serde_ омогућава брзу и компактну серијализацију у _MessagePack_ формат. Модули садрже метаподатке о увозима и дефинисаним ставкама. Апстракција _ItemRef_ обезбеђује интегритет референци између модула.

Структуре _ItemId_ и _StringId_ дефинишу идентификаторе. Централизовани _StringPool_ управља _StringId_ вредностима. _StringPool_ елиминише дуплирање стрингова и убрзава њихово поређење. _ItemRef_ повезује контекст модула преко _StringId_ са _ItemId_ ставке. Статичко разрешавање зависности претходи генерисању кода.

== Генерисање података
Генерисање података ослања се на концепт лење евалуације (енг. _lazy evaluation_). Структура _RecordGenerator_ имплементира стандардни интерфејс итератора @ruststd. Итератор генерише записе секвенцијално на експлицитан захтев. Систем обрађује листу директива и конструише појединачне записе преко функције _generate_record_. Алгоритам користи интерни евалуатор за динамичко израчунавање вредности поља током итерације. Имплементација елиминише потребу за учитавањем комплетног скупа података у радну меморију.

Особина (енг. trait) _FileGenerator_ (листинг @lst:file_generator) дефинише апстракцију за серијализацију генерисаних структура. Интерфејс захтева дефинисање екстензије датотеке и имплементацију методе за генерисање појединачног записа. Опционе методе омогућавају додавање заглавља, подножја и сепаратора између записа. Систем изворно подржава упис у CSV, JSON, XML и SQL формате кроз засебне имплементације дефинисане особине. Сваки формат подржава динамичку инстанцијацију путем конфигурационе мапе објеката. Конфигурација омогућава прилагођавање атрибута попут сепаратора, увлачења текста или имена SQL табеле. Функција _write_records_ преузима итератор и уписује форматиране линије у излазну датотеку на диску.

#figure(
    ```rust
    pub type Record = IndexMap<String, Object>;
    pub trait FileGenerator: fmt::Debug {
        fn generate(&self, record: &Record) -> Result<String, GeneratorError>;
        fn extension(&self) -> &str;
        fn generate_header(&self, fields: &[String]) -> Option<String> { None }
        fn generate_footer(&self) -> Option<String> { None }
        fn needs_separator(&self) -> bool { false }
        fn separator(&self) -> &str { "" }
    }

    ```,
    caption: [Дефиниција особине _FileGenerator_ за генерисање излазних формата.],
) <lst:file_generator>


== Језички сервер
Комуникација са развојним окружењима одвија се кроз LSP. Сервер омогућава анализу изворног кода, навигацију кроз дефиниције и преглед упозорења у реалном времену. Имплементација користи библиотеку _tower-lsp_.

Структура _Workspace_ управља документима. Приступ садржају датотека обезбеђује механизам за синхронизацију _RwLock_. Документ обједињује текст, верзију и структуру _Analysis_. Анализа садржи AST, табелу симбола, референце и листу дијагностичких порука.

Компонента AnalysisEngine координише превођење: 
- лексичку и синтаксну анализу, 
- резолуцију увоза модула кроз _ModuleResolver_ и 
- семантичку анализу путем _SemanticAnalyser_-а.

Метода _update_ (листинг @lst:lsp_update) повезује фазе компилације са LSP догађајима. Анализа се покреће при свакој измени текста. Резултати се мапирају у структуру документа, док се дијагностичке поруке преводе у LSP формат. Раздвајање логике анализе од приказивања омогућава приказ грешака у едитору без прекида рада сервера.

#figure(
```rs
    pub async fn update(
        &self, 
        uri: Url, 
        text: String, 
        version: i32
    ) -> Vec<lsp_types::Diagnostic> {
        let file_path = uri.to_file_path().ok();
        let result = AnalysisEngine::analyse(&text, file_path.as_deref());

        let document = match (result.ast, result.symbol_table) {
            (Some(ast), Some(symbols)) => {
                Document::new(uri.clone(), text, version).with_analysis(Analysis {
                    // ... креирање анализе
                })
            }
            _ => Document::new(uri.clone(), text, version),
        };

        self.insert(uri.clone(), document).await;

        result.diagnostics.into_iter()
            .map(Diagnostic::to_lsp_diagnostics)
            .collect()
    }
```,
caption: [Ажурирање документа и синхронизација анализе],
) <lst:lsp_update>

== Командни интерфејс
Реализација CLI-a користи библиотеку _clap_ @clap. Библиотека омогућава декларативно дефинисање аргумената кроз _Rust_ структуре и енумерације. Мапирање корисничког улаза у типске вредности елиминише потребу за ручним парсирањем стрингова. Систем детектује неисправне уносе током фазе превођења кода, што повећава поузданост програма.

Архитектура почива на енумерацији _Command_. Енумерација дефинише све доступне операције система. Свака варијанта поседује структуру аргумената. Дизајн омогућава јасну декомпозицију логике (листинг @lst:cli_struktura).

#figure(
```rust
    #[derive(Parser)]
    #[command(name = "testa")]
    pub struct Cli {
        #[command(subcommand)]
        pub command: Command,
    }

    #[derive(Subcommand)]
    pub enum Command {
        Generate { ... },
        Compile { ... },
        Init { ... },
        // ... остале CLI команде
    }
```,
caption: [Структура CLI команди],
) <lst:cli_struktura>

Функција _run_ координише извршавање. Систем акумулира _Diagnostic_ поруке приликом појаве грешака. Поруке се форматирају за корисника. Програм враћа повратни код за интеграцију са системским алатима.